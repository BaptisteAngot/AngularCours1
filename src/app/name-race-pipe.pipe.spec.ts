import { NameRacePipePipe } from './name-race-pipe.pipe';

describe('NameRacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NameRacePipePipe();
    expect(pipe).toBeTruthy();
  });
});
